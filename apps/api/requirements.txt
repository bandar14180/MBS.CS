fastapi==0.141.1
# Starlette pinned explicitly to a security-fixed release (transitive via FastAPI).
# 0.38.6 carried PYSEC-2026-1941/2280/2281 (fixed in 0.47.2 / 1.1.0); FastAPI 0.141.1
# requires starlette>=0.46.0, so 1.4.x is the compatible, fully-patched line.
starlette==1.4.1
uvicorn[standard]==0.30.6
pydantic-settings==2.5.2
# Was only a transitive dep of pydantic-settings (its env_file=".env" support). Pinned
# directly now that core/config.py's _dotenv_snapshot() imports it itself, to compose
# DATABASE_URL from discrete DB_HOST/DB_USER/... vars that live only in .env (see that
# function's docstring).
python-dotenv==1.2.2
email-validator==2.2.0
sqlalchemy[asyncio]==2.0.35
# Phase 0 MySQL cutover: asyncpg/psycopg2-binary (Postgres) -> aiomysql (async, used by the
# app's async engine) + PyMySQL (aiomysql's sync-protocol dependency; also used directly by
# Alembic's sync engine in db/migrations/env.py). The `cryptography` package already pinned
# below (for MFA/Fernet) doubles as the dependency MySQL 8's default caching_sha2_password
# auth plugin needs.
aiomysql==0.3.2
pymysql==1.2.0
alembic==1.13.2
celery==5.4.0
redis==5.0.8
boto3==1.35.24
pyjwt==2.13.0
bcrypt==4.2.1
# MFA (Sprint 1): TOTP codes + Fernet encryption of the stored MFA secret.
# cryptography pinned >=50.0.0: 43.0.1 carried 6 advisories (PYSEC-2026-35/1284/2141/3553/3554,
# GHSA-537c-gmf6-5ccf) and the 44.0.0-49.x range carries PYSEC-2026-3552 (PKCS#7 decrypt oracle,
# fixed in 50.0.0). MBS.CS uses only Fernet (never pkcs7_decrypt_*), but we do not allowlist CVEs.
pyotp==2.9.0
cryptography==50.0.0
anthropic==0.34.2
reportlab==4.2.2
prometheus-client==0.21.0
pytest==9.1.1
pytest-cov==7.1.0
httpx==0.27.2
